<%= f.input :name %>
<%= f.error_span(:name) %>

<%= f.button :submit, :class => 'btn-primary' %>
<%= link_to t(:cancel, :default => t("helpers.links.cancel")),
              group_products_indices_path(group_id: current_user.group_id), :class => 'btn btn-default' %>

